# Optional overrides — docker-compose.yml already has safe defaults for dev.
# Copy to .env and edit only what you need to change.

# A strong random string for JWT signing (required in production)
SECRET_KEY=replace-with-a-long-random-secret-key
